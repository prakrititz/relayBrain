// The add-workspace flow is a full window now, not a modal.
// Kept as a re-export so any lingering import of the old name keeps working.
export { AddWorkspaceWindow as AddWorkspaceModal } from "./AddWorkspaceWindow";
